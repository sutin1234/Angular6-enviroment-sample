(function(window) {
    window.__env = window.__env || {}

    // API url
    window.__env.apiUrl = 'http://api.thinnycode.com'
    window.__env.proxyApi = 'http;//proxy.thinnycode.com'
    window.__env.HOST = 'localhost'
    window.__env.PWD = 'root'
    window.__env.BD = 'wp_api'
        // Whether or not to enable debug mode
        // Setting this to false will disable console output
    window.__env.enableDebug = true
}(this))